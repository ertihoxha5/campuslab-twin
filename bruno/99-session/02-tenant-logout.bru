meta {
  name: Logout tenant
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth/logout
  body: none
  auth: none
}

tests {
  test("Logout-i revokon sesionin", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
