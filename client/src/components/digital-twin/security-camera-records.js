export function buildCameraRecords(zones=[]) { return zones.map((zone,index)=>({id:`camera-${zone.sourceId}`,name:`Kamera ${zone.code||index+1}`,code:`CAM-${zone.code||index+1}`,type:"security_camera",zoneId:zone.id,zoneName:zone.name,status:"online",position:[zone.center[0]-zone.size[0]/2+.2,Math.max(2.35,zone.wallHeight-.25),zone.center[1]-zone.size[1]/2+.2],rotation:[0,Math.PI*.25,0],lastUpdate:new Date().toISOString()})); }
